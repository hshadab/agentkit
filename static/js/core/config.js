// Configuration settings for the AgentKit UI
// This imports from the centralized browser configuration
import { config as centralConfig } from '../../../config/browser.js';

// Export the centralized config for backward compatibility
export const config = centralConfig;